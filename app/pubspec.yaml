name: vspo_music
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # --- VSpo Music additions ---
  # PHASE 4: no Dart-side audio or WebView packages needed anymore.
  # Playback now happens entirely on the native Android side
  # (OverlayService.kt hosts a plain android.webkit.WebView inside an
  # invisible system overlay window), reached from Dart via a plain
  # MethodChannel (flutter/services, already part of the Flutter SDK).
  # Earlier phases used just_audio/just_audio_background (Phase 1, blocked by
  # YouTube's PO-token extraction wall) and flutter_inappwebview
  # (Phase 3, blocked by Android tearing down the video Surface on
  # backgrounding) — both removed since neither is used now.

  # PHASE 6 (perf): plain Image.network was decoding every thumbnail at its
  # full downloaded resolution (vspodex.app's thumbnails are often
  # 1280x720+) just to display it at ~48x48 in the list, for all ~340 songs
  # as they scroll through the viewport — expensive to decode, and heavy on
  # Flutter's in-memory image cache, which is what caused the low-fps/janky
  # scrolling. cached_network_image lets us decode straight to the small
  # display size (memCacheWidth/Height) and also disk-caches thumbnails so
  # re-opening the app doesn't re-download and re-decode all of them again.
  cached_network_image: ^3.4.1
  # Where the last successfully downloaded catalog.json is kept, so a launch
  # with no/slow network still gets loudness data and the full song list
  # instead of the older copy bundled in the APK. Already in the dependency
  # tree via cached_network_image; listed here because main.dart uses it.
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates the Android (and iOS) launcher icon from assets/icon/vspo_icon.png
  # — run `dart run flutter_launcher_icons` after `flutter pub get` to apply it
  # (see flutter_launcher_icons config below). Replaces the default Flutter icon.
  flutter_launcher_icons: ^0.14.3

# Config for the flutter_launcher_icons package (dev dependency above).
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/vspo_icon.png"
  # The source image is a full square with a white background already, so it
  # works as-is for the legacy icon. min_sdk_android enables the newer
  # adaptive-icon format too (Android 8+), reusing the same image.
  min_sdk_android: 21
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/vspo_icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled catalog data, produced by catalog-scraper/scrape.js (run against
  # vspodex.app's public /music page — its API endpoint is disallowed by
  # robots.txt, so this is read from a real rendered page, not called
  # directly). Bundling it as an asset means the app works fully offline;
  # re-run the scraper and re-copy catalog.json here whenever the vspodex.app
  # catalog changes, then rebuild.
  assets:
    - assets/catalog.json
    - assets/branding/vspo_logo.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
